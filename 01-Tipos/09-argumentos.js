//Los argumentos son los valores que se pasan a una función cuando se llama.
// Estos valores se utilizan dentro de la función para realizar operaciones o cálculos.

// Definición de una función que suma dos números
// y devuelve el resultado
function suma (a, b) {
    return a + b;
}

let resultado = suma(5, 6);
console.log(resultado);
console.log(typeof suma);

